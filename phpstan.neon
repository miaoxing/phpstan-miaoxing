includes:
    - extension.neon

parameters:
    level: 0

    ignoreErrors:
      - "#^(.+?) is not covered by backward compatibility promise\\. The (class|method) might change in a minor PHPStan version\\.$#"

    paths:
      - src
      - tests
